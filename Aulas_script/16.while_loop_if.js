// Estrutura de Repetição - While - Conditional IF ELSE

const ler = require("readline-sync");

let num1 = 0;
let num = ler.questionInt("Digite um némero: ");

while (num1 <= num2) {
   
    if (num === 101) {
        console.log("Contagem pausada! Limite até 100!");
        break;
    } else {
        console.log(num1);
    }

}